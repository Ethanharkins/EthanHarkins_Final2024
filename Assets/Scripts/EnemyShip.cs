using UnityEngine;

public class EnemyShip : MonoBehaviour
{
    // This script can hold properties or methods related to the ship's behavior
    // Currently, it's empty because the ship is static and doesn't follow the player
}
